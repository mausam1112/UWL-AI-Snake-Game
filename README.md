# Appying `Reinforcement Learning` to Play Snake Game

---

## Training
```shell
# Run Training from scratch on windows
python train.py
# Run Training from scratch on unix 
python3 train.py

# Run Training from last checkpoint (if available) on windows
python train.py --resume
# Run Training from last checkpoint (if available) on unix 
python3 train.py --resume
```

## AI Game Play
```shell
# Run Training from scratch on windows
python snake_game_ai.py
# Run Training from scratch on unix 
python3 snake_game_ai.py
```